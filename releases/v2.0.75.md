
# Release 2.0.75 (2026-05-30)

## Chores

- **re-sync PR head with develop to clear stale out-of-date state**

## Features

- **let model cascade from a top-level identities.toml default**
Add a top-level 'model' key to IdentityConfig and a resolve_model() helper so the model resolves CLI --model -> identity model -> top-level model -> none, mirroring how vergil/vergil-vm cascade. vrg-vm session now uses resolve_model(config, identity, args.model). Ref #1318.
