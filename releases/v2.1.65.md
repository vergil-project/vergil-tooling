
# Release 2.1.65 (2026-06-24)

## Features

- **multiple VM instances per repo (named instances) (#1837)**
* docs(vm): tooling-side design for named VM instances

Tooling companion (#1831) to the authoritative vergil-vm #242 design. Grounds the named-instances model in the actual vrg-vm code and records three keystone decisions from brainstorming: keep per-target delimiters and append the fourth handle segment (the slug's '--' is the readable handle, not the Lima name, which Lima rejects); key the tofu state path on the readable slug while the cloud resource name becomes a deterministic vrg-<hash>; greenfield, no migration. Specifies parsing of the per-identity [vm.<identity>.instances.<name>] namespace, tier-5 composition, --name across the verbs, recorded-state lifecycle dispatch (the Problem 1 fix), and the list INSTANCE/no-vm surface.
