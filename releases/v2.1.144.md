
# Release 2.1.144 (2026-07-15)

## Documentation

- **reflect shipped cloud PR relay in contract and site docs (#2383)**
The GitHub relay (epic vergil-project/.github#148, issue #1858 Deliverable B) has shipped and released in v2.1.143. report-ready always mirrors the ready-state onto refs/vergil/pr-workflow/<branch>; vrg-submit-pr <branch> [<branch> ...] opens PRs worktree-free from the relay ref; vrg-finalize-pr deletes the relay ref on cleanup and sweeps orphans.

Rewrite the CLAUDE.md 'Cloud session prompt contract' so the cloud can do PR-development end-to-end (implement, push, report-ready), with only submission and merge staying human-on-Mac. Remove the retired 'cloud is triage-only / not for PR-development' boundary and the 'until the relay lands' framing.

Update site docs (submit-pr, finalize-pr references, cli-tools-overview, git-workflow guide) to cover report-ready always-push, the vrg-submit-pr branch-list relay path, and relay-ref cleanup in finalize. Note that the relay ref is world-readable on a public repo, so title/summary/notes must carry no secrets.
