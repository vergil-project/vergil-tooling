
# Release 2.0.43 (2026-05-24)

## Bug fixes

- **source credential env file in session bash -c wrapper**
The session command uses bash -c which is non-interactive and does not source .bashrc. Explicitly source ~/.config/vergil/claude.env before cd/exec so CLAUDE_CODE_OAUTH_TOKEN is available to processes launched via vrg-vm session.


## Chores

- **bump version to 2.0.43**
